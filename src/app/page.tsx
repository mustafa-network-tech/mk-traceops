import type { Metadata } from "next"; import { redirect } from "next/navigation"; import { LandingPage } from "@/components/landing/landing-page"; import { isPlatformAdmin } from "@/lib/rbac/helpers"; import { getRbacSession } from "@/lib/rbac/session-server";
export const dynamic="force-dynamic"; export const metadata:Metadata={title:"MK TraceOps — Tanıtım",description:"Üretimden sevkiyata tek panelde operasyon."};
export default async function Home(){const ctx=await getRbacSession();if(!ctx)return <LandingPage/>;if(ctx.user.status==="active")redirect(isPlatformAdmin(ctx.user)?"/platform":"/kokpit");redirect("/basvuru-bekleniyor");}
