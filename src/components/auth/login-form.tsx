"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react";
import { signInAction } from "@/app/actions/auth"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { toastActionError } from "@/lib/client/action-toast";
export function LoginForm(){const router=useRouter();const[pending,setPending]=useState(false);return <form className="space-y-4" onSubmit={async(e)=>{e.preventDefault();setPending(true);const fd=new FormData(e.currentTarget);try{const r=await signInAction(String(fd.get("email")??""),String(fd.get("password")??""));if(!r.ok){toastActionError(r.error);return;}router.push("/kokpit");router.refresh();}catch{toastActionError("Giriş sırasında hata oluştu.");}finally{setPending(false);}}}><div className="space-y-1.5"><Label htmlFor="login-email">E-posta</Label><Input id="login-email" name="email" type="email" autoComplete="email" required/></div><div className="space-y-1.5"><Label htmlFor="login-password">Şifre</Label><Input id="login-password" name="password" type="password" autoComplete="current-password" required/></div><Button type="submit" className="w-full" disabled={pending}>{pending?"Giriş yapılıyor…":"Giriş yap"}</Button><p className="text-center text-xs text-slate-600">Yeni fabrika mı? <Link href="/kayit" className="text-violet-700 hover:underline">Başvuru oluştur</Link></p></form>}
