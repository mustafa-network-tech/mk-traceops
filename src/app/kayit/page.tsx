import { redirect } from "next/navigation"; import { RegisterFactoryForm } from "@/components/auth/register-factory-form"; import { brand } from "@/lib/constants/brand"; import { isPlatformAdmin } from "@/lib/rbac/helpers"; import { getRbacSession } from "@/lib/rbac/session-server";
export const dynamic="force-dynamic";
export default async function KayitPage(){const ctx=await getRbacSession();if(ctx?.user.status==="active")redirect(isPlatformAdmin(ctx.user)?"/platform":"/kokpit");return <div className="flex min-h-screen items-center justify-center bg-slate-100 p-6"><div className="w-full max-w-md rounded-xl border bg-white p-8 shadow-sm"><p className="font-mono text-sm font-semibold">{brand.name}</p><h1 className="mt-1 text-xl font-semibold">Fabrika başvurusu</h1><p className="mt-1 text-sm text-slate-600">Başvurunuz onaylandığında hesabınız etkinleşir.</p><div className="mt-6"><RegisterFactoryForm/></div></div></div>}
